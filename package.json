{
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo build",
    "db:generate": "turbo db:generate",
    "dev": "turbo dev --parallel",
    "dev:web": "turbo dev:web --parallel",
    "dev:window": "turbo dev:window --parallel",
    "lint": "turbo lint"
  },
  "devDependencies": {
    "prettier": "^2.5.1",
    "turbo": "latest"
  }
}
